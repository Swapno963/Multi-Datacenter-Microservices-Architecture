events {}
http {
    server {
        listen 8082;
        location / {
            default_type application/json;
            root /usr/share/nginx/html;
            index order.json;
        }
    }
}
