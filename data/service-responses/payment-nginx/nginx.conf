events {}
http {
    server {
        listen 8083;
        location / {
            default_type application/json;
            root /usr/share/nginx/html;
            index payment.json;
        }
    }
}
