events {}
http {
    server {
        listen 8080;
        location / {
            default_type application/json;
            root /usr/share/nginx/html;
            index user.json;
        }
    }
}
