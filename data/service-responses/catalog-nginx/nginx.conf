events {}
http {
    server {
        listen 8081;
        location / {
            default_type application/json;
            root /usr/share/nginx/html;
            index catalog.json;
        }
    }
}
