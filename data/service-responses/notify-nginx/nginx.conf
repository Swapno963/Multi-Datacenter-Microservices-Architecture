events {}
http {
    server {
        listen 8084;
        location / {
            default_type application/json;
            root /usr/share/nginx/html;
            index notify.json;
        }
    }
}
