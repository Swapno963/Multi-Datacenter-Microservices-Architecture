events {}
http {
    server {
        listen 8085;
        location / {
            default_type application/json;
            root /usr/share/nginx/html;
            index analytics.json;
        }
    }
}
